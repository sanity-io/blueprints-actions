{
  "name": "@sanity/runtime-actions",
  "version": "0.0.1",
  "description": "GitHub Action to interact with Sanity blueprints using @sanity/runtime-cli",
  "main": "dist/index.js",
  "private": false,
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "tap --show-full-coverage --include=test/**/*.test.js"
  },
  "keywords": [
    "github-action",
    "sanity",
    "blueprints",
    "deploy",
    "functions",
    "runtime"
  ],
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "@vercel/ncc": "^0.38.4",
    "mock-require": "^3.0.3",
    "tap": "^21.1.1"
  }
}
